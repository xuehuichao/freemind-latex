language: python
python:
  - "2.7"

virtualenv:
  system_site_packages: true

# command to install dependencies
install:
  - "pip install bibtexparser python-gflags pypdf2"
  
# command to run tests
script: "python -m unittest discover tests"
